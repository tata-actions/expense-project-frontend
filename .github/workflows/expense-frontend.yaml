name: feature-pipeline-frontend

on: [push]
permissions:
  contents: read
  statuses: write
  security-events: read

jobs:
  feature-pipeline:
    if: github.ref != 'refs/heads/main'
    uses: tata-actions/workflows/.github/workflows/dummy-feature-frontend.yml@main
    with:
      project: expense
      component: frontend
      environment: dev
    secrets: inherit  

  
    
